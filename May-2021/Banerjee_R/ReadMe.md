
Python code to load currency conversion data from website to SQL DB

This code loads the currency conversion values from the website to a table.

Scheduler.py is the main code doing the job for others
